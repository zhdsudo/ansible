location ~* ^/wp-includes.*\.(gif|jpeg|jpg|txt|png|tif|tiff|ico|jng|bmp|doc|pdf|rtf|xls|ppt|rar|rpm|swf|zip|bin|exe|dll|deb|cur)$
{
    root /var/www/site.ru;   
    try_files $uri $uri/ @fallback;
    
}
